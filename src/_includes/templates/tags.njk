{% set posts = search.pages("type=posts") %}
{% for post in posts %}
  {% if post.data.tags %}
    {% for tag in post.data.tags %}
      <span class="tag">{{ tag }}</span>
    {% endfor %}
  {% endif %}
{% endfor %}